cachito:
  enabled: true
  packages:
    gomod:
    - path: .
content:
  source:
    dockerfile: Dockerfile.konflux
    git:
      branch:
        target: backplane-2.11
      url: git@github.com:openshift-priv/image-based-install-operator.git
      web: https://github.com/openshift/image-based-install-operator
    public_upstream:
      url: https://github.com/openshift/image-based-install-operator
      branch:
        target: backplane-2.11
distgit:
  component: mce-image-based-install-operator-container
  branch: rhaos-{MAJOR}.{MINOR}-rhel-9
delivery:
  delivery_repo_names:
  - multicluster-engine/image-based-install-operator-rhel9
for_payload: false
enabled_repos:
- rhel-9-baseos-rpms
- rhel-9-appstream-rpms
from:
  builder:
    - stream: rhel-9-golang-1.25
  stream: rhel9
name: multicluster-engine/image-based-install-operator-rhel9
owners:
- acm-cicd@redhat.com
jira:
  project: ACM
  component: mce-image-based-install-operator-container
