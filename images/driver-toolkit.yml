content:
  source:
    dockerfile: Dockerfile
    modifications:
    - action: replace
      match: "ARG RHEL_VERSION=''"
      replacement: "ARG RHEL_VERSION='9.2'"

    # Uncomment the following sections to pin specific kernel versions

    # - action: replace
    #   match: "ARG KERNEL_VERSION=''"
    #   replacement: "ARG KERNEL_VERSION='1.2.3'"

    # - action: replace
    #   match: "ARG RT_KERNEL_VERSION=''"
    #   replacement: "ARG RT_KERNEL_VERSION='1.2.3'"
    git:
      # branch:
        # target: release-{MAJOR}.{MINOR}
      # url: git@github.com:openshift-priv/driver-toolkit.git
      # web: https://github.com/openshift/driver-toolkit

      # Fork disables installation of abi since it not in the CentOS Stream 9 RPMs.
      branch:
        target: release-4.14-no-abi
      url: git@github.com:jupierce/driver-toolkit.git
      web: https://github.com/jupierce/driver-toolkit
enabled_repos:
- rhel-9-server-ose-rpms-embargoed
- rhel-9-baseos-rpms
- rhel-9-appstream-rpms
- rhel-92-baseos-rpms
- rhel-92-appstream-rpms
- rhel-92-rt-rpms
for_payload: true
from:
  member: openshift-enterprise-base-rhel9
name: openshift/ose-driver-toolkit
owners:
- edge-sro@redhat.com
