base_only: true
content:
  source:
    git:
      branch:
        target: openshift-base-ruby
      url: git@github.com:openshift/ocp-build-data.git
# TODO: it SHOULD be:
#distgit:
  #branch: rhaos-{MAJOR}.{MINOR}-rhel-8
# but the idiot who requested the repo didn't ask for this branch; do ^^ once
# https://projects.engineering.redhat.com/browse/CLOUDBLD-5166 is complete...
enabled_repos:
- rhel-8-baseos-rpms
- rhel-8-appstream-rpms
for_payload: false
for_release: false
from:
  image: registry.redhat.io/ubi8/ruby-25:1-122
name: openshift/base-ruby
owners:
- aos-team-art@redhat.com
