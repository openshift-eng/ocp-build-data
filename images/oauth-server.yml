container_yaml:
  go:
    modules:
    - module: github.com/openshift/oauth-server
content:
  source:
    dockerfile: images/Dockerfile.rhel
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/oauth-server.git
enabled_repos:
- rhel-server-rpms
- rhel-server-optional-rpms
from:
  builder:
  - stream: golang-1.12
  member: openshift-enterprise-base
name: openshift/ose-oauth-server
owners:
- mfojtik@redhat.com
