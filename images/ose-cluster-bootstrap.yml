container_yaml:
  go:
    modules:
    - module: github.com/openshift/cluster-bootstrap
content:
  source:
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/cluster-bootstrap.git
enabled_repos:
- rhel-server-rpms
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-cluster-bootstrap
owners:
- aos-master@redhat.com
