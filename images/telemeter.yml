container_yaml:
  go:
    modules:
    - module: github.com/openshift/telemeter
content:
  source:
    dockerfile: Dockerfile
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/telemeter.git
enabled_repos:
- rhel-server-optional-rpms
- rhel-server-rpms
for_payload: true
from:
  builder:
  - stream: golang-1.11
  member: openshift-enterprise-base
name: openshift/ose-telemeter
owners:
- team-monitoring@redhat.com
non_shipping_repos:
- rhel-server-optional-rpms
