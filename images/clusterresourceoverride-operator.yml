mode: wip
container_yaml:
  go:
    modules:
    - module: github.com/openshift/cluster-resource-override-admission-operator
content:
  source:
    dockerfile: Dockerfile.rhel7
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/cluster-resource-override-admission-operator
enabled_repos:
- rhel-server-rpms
- rhel-server-optional-rpms
distgit:
  component: ose-clusterresourceoverride-operator-container
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-clusterresourceoverride-operator
owners:
- akashem@redhat.com
update-csv:
  manifests-dir: manifests/
  registry: image-registry.openshift-image-registry.svc:5000
