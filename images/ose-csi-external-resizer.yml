container_yaml:
  go:
    modules:
    - module: github.com/openshift/csi-external-resizer
content:
  source:
    dockerfile: Dockerfile.openshift
    git:
      branch:
        # Remove fallback when https://github.com/openshift/csi-external-resizer/issues/104 is addressed.
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/csi-external-resizer.git
enabled_repos:
- rhel-server-rpms
for_payload: 'false'
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-csi-external-resizer
owners:
- aos-storage-staff@redhat.com
