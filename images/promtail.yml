mode: wip
container_yaml:
  go:
    modules:
    - module: https://github.com/openshift/loki
content:
  source:
    dockerfile: Dockerfile.promtail.ocp
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/loki.git
enabled_repos:
- rhel-server-rpms
- rhel-server-optional-rpms
from:
  builder:
  - stream: golang-1.12
  member: openshift-enterprise-base
name: openshift/ose-promtail
owners:
- fbranczy@redhat.com
