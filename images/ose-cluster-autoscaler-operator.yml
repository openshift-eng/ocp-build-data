content:
  source:
    dockerfile: Dockerfile.rhel
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift-priv/cluster-autoscaler-operator.git
      web: https://github.com/openshift/cluster-autoscaler-operator
    ci_alignment:
      streams_prs:
        ci_build_root:
          stream: rhel-9-golang-ci-build-root
distgit:
  branch: rhaos-{MAJOR}.{MINOR}-rhel-9
  component: ose-cluster-autoscaler-operator-container
for_payload: true
from:
  builder:
  - stream: rhel-9-golang
  member: openshift-enterprise-base-rhel9
name: openshift/ose-cluster-autoscaler-rhel9-operator
payload_name: cluster-autoscaler-operator
owners:
- aos-cloud@redhat.com
konflux:
  tagging_mode: enabled