mode: wip
content:
  source:
    dockerfile: Dockerfile.cephfs
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/external-storage.git
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-cephfs-provisioner
owners:
- mawong@redhat.com
