content:
  source:
    git:
      branch:
        # At present, this repo does not use the release- branch pattern. Remove fallback when it does.
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift-priv/service-idler.git
    modifications:
    - action: replace
      match: origin-service-idler
      replacement: atomic-openshift-service-idler
    path: images/service-idler
enabled_repos:
- rhel-server-rpms
- rhel-server-ose-rpms-embargoed
for_payload: false
from:
  member: openshift-enterprise-base
labels:
  License: GPLv2+
  io.k8s.description: This is a component of OpenShift Container Platform which controls
    the idling and unidling scalable resources
  io.k8s.display-name: OpenShift Container Platform Service Idler
  io.openshift.tags: openshift
  vendor: Red Hat
name: openshift/ose-service-idler
non_shipping_repos:
- rhel-server-ose-rpms-embargoed
owners:
- sross@redhat.com
