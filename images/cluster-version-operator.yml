container_yaml:
  go:
    modules:
    - module: github.com/openshift/cluster-version-operator
content:
  source:
    dockerfile: Dockerfile.rhel7
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/cluster-version-operator.git
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-cluster-version-operator
owners:
- crawford@redhat.com
