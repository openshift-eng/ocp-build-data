content:
  source:
    dockerfile: Dockerfile.openshift
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift-priv/multus-admission-controller.git
      web: https://github.com/openshift/multus-admission-controller
    ci_alignment:
      streams_prs:
        # Label so that this PR automatically merges if tests pass.
        auto_label:
        - approved
        - lgtm
        ci_build_root:
          stream: rhel-8-golang-ci-build-root
enabled_repos:
- rhel-8-baseos-rpms
- rhel-8-appstream-rpms
for_payload: true
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-multus-admission-controller
owners:
- multus-dev@redhat.com
comet:
- type: repository
  name: openshift4/ose-multus-admission-controller
  id: 5cd9c8775a13467289f4d5e1
