container_yaml:
  go:
    modules:
    - module: github.com/openshift/ptp-operator
content:
  source:
    dockerfile: must-gather/Dockerfile.rhel7
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift-priv/ptp-operator.git
dependents:
- ptp-operator
enabled_repos:
- rhel-server-rpms
- rhel-server-extras-rpms
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ptp-operator-must-gather
owners:
- multus-dev@redhat.com
