container_yaml:
  go:
    modules:
    - module: github.com/openshift/external-storage
content:
  source:
    dockerfile: Dockerfile.efs
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/external-storage.git
enabled_repos:
- rhel-server-optional-rpms
- rhel-server-rpms
for_payload: false
from:
  builder:
  - stream: golang-1.11
  member: openshift-enterprise-base
name: openshift/ose-efs-provisioner
owners:
- mawong@redhat.com
non_shipping_repos:
- rhel-server-optional-rpms
