container_yaml:
  go:
    modules:
    - module: github.com/openshift/metallb
content:
  source:
    dockerfile: Dockerfile.openshift
    git:
      branch:
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift-priv/metallb.git
# disabled until upstream is configured for 4.10
# dependents:
# - ose-metallb-operator
for_payload: false
from:
  builder:
  - stream: golang
  member: openshift-base-rhel8
name: openshift/ose-metallb
owners:
- metallb-dev@redhat.com
