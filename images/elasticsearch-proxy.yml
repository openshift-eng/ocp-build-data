mode: wip
container_yaml:
  go:
    modules:
    - module: github.com/openshift/elasticsearch-proxy
content:
  source:
    dockerfile: Dockerfile
    git:
      branch:
        # At this time, this repo only has a master branch. When it follows convention, remove fallback.
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/elasticsearch-proxy.git
distgit:
  component: ose-elasticsearch-proxy-container
enabled_repos:
- rhel-server-rpms
- rhel-server-optional-rpms
from:
  builder:
  - stream: golang
  member: openshift-enterprise-base
name: openshift/ose-elasticsearch-proxy
owners:
- aos-logging@redhat.com
