content:
  source:
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/image-registry.git
    specfile: image-registry.spec
name: atomic-openshift-dockerregistry
owners:
- openshift-image-registry@redhat.com
