## NOTE: probably nothing needs this but we have shipped in the past and might as well continue to
content:
  source:
    git:
      branch:
        fallback: master
        target: release-{MAJOR}.{MINOR}
      url: git@github.com:openshift/service-idler.git
    specfile: service-idler.spec
name: atomic-openshift-service-idler
owners:
- sross@redhat.com
