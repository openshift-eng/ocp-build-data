mode: disabled
# [lmeyer] the spec needs modifications to work properly with tito and it's
# just been easier to do it by hand so far.
content:
  source:
    git:
      branch:
        target: 1.18
      url: git@gitlab.cee.redhat.com:openshift-art/builders/golang-scl-shim.git
    specfile: golang-scl-shim.spec
name: golang-scl-shim
owners:
- aos-team-art@redhat.com
