releases:
  stream:
    assembly:
      type: stream
      permits:
      # why: "Always build shippable" does not yet apply to 4.10
      - code: MISMATCHED_SIBLINGS
        component: '*'
      - code: OUTDATED_RPMS_IN_STREAM_BUILD
        component: 'rhcos'
      - code: CONFLICTING_GROUP_RPM_INSTALLED
        component: 'rhcos'
      - code: OUTDATED_RPMS_IN_STREAM_BUILD
        component: 'ose-ovn-kubernetes'
      - code: OUTDATED_RPMS_IN_STREAM_BUILD
        component: 'ironic-hardware-inventory-recorder-image'
  art3171:
    assembly:
      type: custom
      members:
        images:
        - distgit_key: '*'
          why: test cachito integration for all images
          metadata:
            container_yaml!: {}
            content:
              source:
                pkg_managers!:
                - gomod
