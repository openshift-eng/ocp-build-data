releases:
  stream:
    assembly:
      type: stream
      permits:
      - code: INCONSISTENT_RHCOS_RPMS
        component: 'rhcos'
      - code: OUTDATED_RPMS_IN_STREAM_BUILD
        component: 'rhcos'
      - code: CONFLICTING_GROUP_RPM_INSTALLED
        component: 'rhcos'
